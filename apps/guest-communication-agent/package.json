{
  "name": "guest-communication-agent",
  "version": "0.1.0",
  "description": "Guest-Comms Agent (GCA) — WhatsApp guest communication. Webhook scaffolded here; the actual LangGraph agent (currently in issebya-homes-website, not yet wired to live traffic there either) will be ported in once that repo's in-flight merge is resolved.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "next dev --port 3005",
    "build": "next build",
    "start": "next start --port 3005",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "next": "^16.2.1",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
