{
  "name": "telegram-router-tasks",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 3003",
    "start": "uv run uvicorn app.main:app --port 3003",
    "typecheck": "uv run mypy app",
    "format:check": "uv run ruff format --check ."
  }
}
