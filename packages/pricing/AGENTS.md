# Agent instructions

This package is a single exported constant (`ROOM_PRICING`) with no logic,
no functions, and no state. There are no behavioral rules here beyond the
root `AGENTS.md`'s repo-wide conventions (yarn only, conventional commits,
lefthook, workspace layout). Nothing package-specific to add.
